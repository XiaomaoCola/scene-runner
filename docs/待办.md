# 待办

## 目录结构重构

### configs/intents/ 和 data/templates/builder_base/ 的命名混淆

**问题：**  
`configs/intents/` 和 `data/templates/builder_base/` 下的子目录以 Intent 命名（如 `BuilderBaseAttack/`），但里面存放的实际上是 Plan 层的展开内容（各阶段坐标、模板图片）。Intent 和 Plan 是不同的架构层，现在的目录结构模糊了两者的边界。

**期望：**  
目录应反映 Plan 层，例如改为 `configs/plans/` 和 `data/templates/builder_base/plans/`，子目录以 Plan 命名。

**进度：**  
- [x] `data/templates/builder_base/BuilderBaseAttackIntent/` → `BuilderBaseAttackPlan/`
- [x] `data/templates/builder_base/BuilderBaseCollectResources/` → `BuilderBaseCollectResourcesPlan/`
- [ ] `configs/intents/BuilderBaseAttack/` → 待重命名
- [ ] `configs/intents/BuilderBaseCollectResources/` → 待重命名
- [ ] `configs/intents/` 顶层目录本身 → 考虑改为 `configs/plans/`
